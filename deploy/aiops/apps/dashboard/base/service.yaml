apiVersion: v1
kind: Service
metadata:
  name: aiops-dashboard
  labels:
    app.kubernetes.io/name: aiops-dashboard
spec:
  selector:
    app.kubernetes.io/name: aiops-dashboard
  ports:
    - name: http
      port: 8010
      targetPort: http
