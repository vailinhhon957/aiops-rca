apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aiops-dashboard-state
  labels:
    app.kubernetes.io/name: aiops-dashboard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
